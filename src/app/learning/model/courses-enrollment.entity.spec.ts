import { CoursesEnrollment } from './courses-enrollment.entity';

describe('CoursesEnrollment', () => {
  it('should create an instance', () => {
    expect(new CoursesEnrollment()).toBeTruthy();
  });
});
